# Схема-макет лендингов залов

Краткая структура блоков и визуальная иерархия для лендингов конференц-зала.  
**Генерация:** цепочка агентов 1→9 (keywords → brief → content → editor → publish + images). Шаблон: `prompts/templates/konferenc_zal_page.md`. Стиль как на страницах услуг (`prompts/templates/service_page.md`).

---

## Схема чередования блоков (источник правды)

**Порядок:** два рядом → картинка во всю ширину → ряд: картинка слева, текст справа → ряд: текст слева, картинка справа → текст во всю ширину → картинка во всю ширину.

```mermaid
flowchart TB
    subgraph R1 [1. Два блока в ряд]
        A[Для кого]
        B[Преимущества]
    end
    subgraph R2 [2. Картинка во всю ширину]
        Img1[Галерея / фото зала]
    end
    subgraph R3 [3. Картинка слева | Текст справа]
        Img2[Фото]
        C[Характеристики]
    end
    subgraph R4 [4. Текст слева | Картинка справа]
        D[Оснащение]
        Img3[Фото]
    end
    subgraph R5 [5. Текст во всю ширину]
        E[Тарифы]
        F[Как забронировать]
        G[Форма]
        H[Кейсы]
        I[Отзывы]
        J[FAQ]
    end
    subgraph R6 [6. Картинка во всю ширину]
        Img4[Финальное фото / галерея]
    end
    Hero --> R1 --> R2 --> R3 --> R4 --> R5 --> R6
```

---

## Порядок секций (конференц-зал)

| № | Секция | Вёрстка | Класс |
|---|--------|---------|-------|
| 0 | Hero | Полная ширина | `.landing-hero` |
| 1 | Для кого + Преимущества | Два в ряд | `.landing-row-two-cols` |
| 2 | Галерея | Картинка во всю ширину | `.landing-gallery` |
| 3 | Характеристики | Картинка слева, текст справа | `.landing-features.landing-image-text` |
| 4 | Оснащение | Текст слева, картинка справа | `.landing-equipment.landing-text-image` |
| 5 | Тарифы, Как забронировать, форма, кейсы, отзывы, FAQ | Текст во всю ширину | `.landing-pricing`, `.landing-booking-steps` и др. |
| 6 | Финальное фото | Картинка во всю ширину | `.landing-gallery-end` |

---

## Порядок секций (общий справочник)

| № | Секция | Описание / контент |
|---|--------|---------------------|
| 1 | Hero | 80–100vh, H1, подзаголовок, цена, 2 CTA (primary + outline) |
| 2 | For Whom | Для кого — карточки (в конференц-зале: в одном ряду с Преимуществами) |
| 3 | Benefits | Преимущества — карточки |
| 4 | Features | Характеристики — dl/dt/dd или таблица; вариант: текст + фото |
| 5 | Pricing | Тарифы — 3 карточки |
| 6 | Equipment | Оснащение — список/сетка; вариант: фото + текст |
| 7 | Booking Steps | Как забронировать — нумерованные шаги |
| 8 | Form | Оставить заявку — форма или CTA на форму |
| 9 | Gallery | Как выглядит зал — сетка 3/2/1 колонки |
| 10 | Cases | Кейсы — карточки 2 колонки |
| 11 | Testimonials | Отзывы — карточки 2–3 колонки |
| 12 | FAQ | Частые вопросы — аккордеон |

---

## Визуальная иерархия

- **Hero:** фон (фото зала + overlay 40–60%), H1 белым при фоне-фото, цена выделена жирным/иконкой.
- **H2:** 28–36px (desktop), 24–28px (mobile), margin-top 60–80px, по центру.
- **Карточки:** белый фон, `box-shadow: 0 2px 8px rgba(0,0,0,0.08)`, `border-radius: 8px`, отступы между карточками 20–30px.
- **Кнопки CTA:** min-height 50px, акцентный цвет (teal), в Hero — вторая кнопка outline («Тарифы» или «Подробнее»).
- **Чередование:** два текстовых блока в ряд (`.landing-row-two-cols`), затем галерея, затем блоки «текст+фото» и «фото+текст» — стили в `landing-pages.css` (тема).

---

## Интеграция с WordPress

- **Источник контента:** цепочка агентов 1→9 (keywords → brief → content → editor) → `scripts/publish_konferenc_zal_from_md.py` (MD→HTML) → `post_content` через WP REST API.
- **Шаблон страницы:** «Лендинг Конференц-зал» (`template-page-landing-konferenc-zal.php`). При непустом `post_content` выводится он; иначе — статичный макет темы.
- **Запуск цепочки:** `python scripts/run_konferenc_zal_chain.py`
